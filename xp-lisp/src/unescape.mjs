export function unescape(text) {
  text = text.replace(/{{/g, "${");
  text = text.replace(/}}/g, "}");
  text = text.replace(/<string>/g, "`");
  text = text.replace(/<[/]string>/g, "`");
  text = text.replace(/<%>/g, "`");
  text = text.replace(/<%dot%>/g, `.`);
  text = text.replace(/<%period%>/g, `.`);
  text = text.replace(/<%comma%>/g, `,`);
  text = text.replace(/<%dollar%>/g, `$`);
  text = text.replace(/<%dollar%>/g, `$`);
  text = text.replace(/<%backtick%>/g, "`");
  text = text.replace(/<%colon%>/g, `:`);
  text = text.replace(/<%semi-colon%>/g, `;`);
  text = text.replace(/<%semi%>/g, ";");
  text = text.replace(/<%semi-colon%>/g, `;`);
  text = text.replace(/<%single-quote%>/g, `'`);
  text = text.replace(/<%single%>/g, `'`);
  text = text.replace(/<%double-quote%>/g, `"`);
  text = text.replace(/<%double%>/g, `"`);
  text = text.replace(/<%sharp%>/g, `#`);
  text = text.replace(/<%percent%>/g, `%`);
  text = text.replace(/<%ampersand%>/g, `&`);
  text = text.replace(/<%amp%>/g, `&`);
  text = text.replace(/<%vertical-bar%>/g, `|`);
  text = text.replace(/<%vertical%>/g, `|`);
  text = text.replace(/<%and%>/g, `&&`);
  text = text.replace(/<%or%>/g, `||`);
  text = text.replace(/<%equal%>/g, `=`);
  text = text.replace(/<%equal-operator%>/g, `==`);
  text = text.replace(/<%equal-op%>/g, `==`);
  text = text.replace(/<%not-equal-operator%>/g, `!=`);
  text = text.replace(/<%not-equal-op%>/g, `!=`);
  text = text.replace(/<%tilde%>/g, `~`);
  text = text.replace(/<%slash%>/g, `/`);
  text = text.replace(/<%forward-slash%>/g, `/`);
  text = text.replace(/<%fore-slash%>/g, `/`);
  text = text.replace(/<%backslash%>/g, `\\`);
  text = text.replace(/<%backward-slash%>/g, `\\`);
  text = text.replace(/<%back-slash%>/g, `\\`);
  text = text.replace(/<%caret%>/g, `^`);
  text = text.replace(/<%hat%>/g, `^`);
  text = text.replace(/<%curly-bracket-start%>/g, `{`);
  text = text.replace(/<%curly-start%>/g, `{`);
  text = text.replace(/<%curly-bracket-end%>/g, `}`);
  text = text.replace(/<%curly-end%>/g, `}`);
  text = text.replace(/<%bracket-start%>/g, `[`);
  text = text.replace(/<%bracket-end%>/g, `]`);
  text = text.replace(/<%at%>/g, `@`);
  text = text.replace(/<%at-mark%>/g, `@`);
  text = text.replace(/<%plus%>/g, `+`);
  text = text.replace(/<%minus%>/g, `-`);
  text = text.replace(/<%divide%>/g, `/`);
  text = text.replace(/<%multiply%>/g, `*`);
  text = text.replace(/<%power%>/g, `**`);
  text = text.replace(/<%pow%>/g, `**`);
  text = text.replace(/<%paren-start%>/g, `(`);
  text = text.replace(/<%paren-end%>/g, `)`);
  text = text.replace(/<%question-mark%>/g, `?`);
  text = text.replace(/<%question%>/g, `?`);
  text = text.replace(/<%less-than%>/g, `<`);
  text = text.replace(/<%less%>/g, `<`);
  text = text.replace(/<%lt%>/g, `<`);
  text = text.replace(/<%greater-than%>/g, `>`);
  text = text.replace(/<%greater%>/g, `>`);
  text = text.replace(/<%gt%>/g, `>`);
  text = text.replace(/<%underscore%>/g, `_`);
  text = text.replace(/<%under%>/g, `_`);
  text = text.replace(/<%space%>/g, ` `);
  text = text.replace(/<%tab%>/g, `\\t`);
  text = text.replace(/<%cr%>/g, `\\r`);
  text = text.replace(/<%lf%>/g, `\\n`);
  text = text.replace(/<%null%>/g, `\\0`);
  text = text.replace(/<%exclamation-mark%>/g, `!`);
  text = text.replace(/<%exclamation-point%>/g, `!`);
  text = text.replace(/<%exclamation%>/g, `!`);
  text = text.replace(/<%bang%>/g, ``);
  text = text.replace(/<%shebang%>/g, `#!`);
  text = text.replace(/<#>/g, "");
  return text;
}
